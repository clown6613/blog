---
import BaseLayout from '../layouts/BaseLayout.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';

const { base } = import.meta.env;
---

<BaseLayout title="404 - ページが見つかりません">
  <Header slot="header" />

  <div class="text-center py-20">
    <h1 class="text-9xl font-bold text-gray-300 dark:text-gray-700 mb-4">404</h1>
    <h2 class="text-3xl font-bold text-gray-900 dark:text-white mb-4">
      ページが見つかりません
    </h2>
    <p class="text-lg text-gray-600 dark:text-gray-400 mb-8">
      お探しのページは存在しないか、移動した可能性があります。
    </p>
    <a
      href={`${base}/`}
      class="inline-block px-6 py-3 bg-primary text-white rounded-lg hover:bg-blue-700 transition-colors font-medium"
    >
      ホームに戻る
    </a>
  </div>

  <Footer slot="footer" />
</BaseLayout>
